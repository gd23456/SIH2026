# ---------------------------------------------------------------------------
# Karigar AI — development / CI dependencies
#   pip install -r requirements-dev.txt
# ---------------------------------------------------------------------------

-r requirements.txt

pytest>=8.3,<9
httpx>=0.27,<1          # FastAPI TestClient transport
ruff>=0.6,<1            # linter + formatter
